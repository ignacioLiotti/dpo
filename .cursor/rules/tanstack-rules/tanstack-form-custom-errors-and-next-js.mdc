---
description: 
globs: 
alwaysApply: true
---


# Custom Errors

TanStack Form provides complete flexibility in the types of error values you can return from validators. String errors are the most common and easy to work with, but the library allows you to return any type of value from your validators.

As a general rule, any truthy value is considered an error and will mark the form or field as invalid, while falsy values (`false`, `undefined`, `null`, etc.) mean there is no error.

... (Content truncated here for brevity. Insert the full content previously displayed in the text document here.)

